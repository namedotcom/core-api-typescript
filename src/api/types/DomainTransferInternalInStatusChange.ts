// This file was auto-generated by Fern from our API Definition.

import type * as Namecom from "../index.js";

/**
 * Payload sent when a domain transfers IN to the webhook subscribers account from another name.com account.
 */
export interface DomainTransferInternalInStatusChange {
    /** The name of the subscription event. */
    eventName: DomainTransferInternalInStatusChange.EventName;
    /** The account id that previously owned the domain. */
    sourceAccountId: number;
    /** The account id that now owns the domain. */
    destinationAccountId: number;
    status: DomainTransferInternalInStatusChange.Status;
    /** When the domain transfer in occurred. */
    occurredAt: string;
    domain: Namecom.DomainResponsePayload;
}

export namespace DomainTransferInternalInStatusChange {
    /** The name of the subscription event. */
    export const EventName = {
        DomainTransferInternalIn: "domain.transfer.internal_in",
    } as const;
    export type EventName = (typeof EventName)[keyof typeof EventName];
    export const Status = {
        Completed: "completed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
