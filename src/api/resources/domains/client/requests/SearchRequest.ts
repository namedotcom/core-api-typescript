// This file was auto-generated by Fern from our API Definition.

import type * as Namecom from "../../../../index.js";

/**
 * @example
 *     {
 *         keyword: "mydomain"
 *     }
 */
export interface SearchRequest {
    /** Keyword is the search term to search for. It can be just a word, or a whole domain name. */
    keyword: string;
    /**
     * Timeout is a value in milliseconds on how long to perform the search for. Valid timeouts are between 500ms to 12,000ms. If not specified, timeout defaults to 12,000ms.
     * Since some additional processing is performed on the results, a response may take longer then the timeout.
     */
    timeout?: number;
    /** TLDFilter will limit results to only contain the specified TLDs. There is a maximum of 50 TLDs that can be used in this filter */
    tldFilter?: string[];
    /** Optional. Limits results to the given `purchaseType`. **Recommended:** `registration` for most integrations — omit only if you choose to support acquisition types. See the [Domain purchase pricing guide](/guides/domain-pricing). */
    purchaseType?: Namecom.SearchPurchaseType;
}
