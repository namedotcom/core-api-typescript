// This file was auto-generated by Fern from our API Definition.

import type * as Namecom from "../../../../index.js";

/**
 * @example
 *     {
 *         domainNames: ["domainNames"]
 *     }
 */
export interface AvailabilityRequest {
    /** DomainNames is the list of domains to check if they are available. */
    domainNames: string[];
    /** Optional filter. **Recommended:** `registration` for most integrations — omit only if you support acquisition types. Non-matching domains are returned with `purchasable: false` (Search omits them instead). See the [Domain purchase pricing guide](/guides/domain-pricing). */
    purchaseType?: Namecom.SearchPurchaseType;
}
